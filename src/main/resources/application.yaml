spring:
  application:
    name: mecanica-api
  datasource:
    url: jdbc:postgresql://localhost:5432/mecanicadb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
#      ddl-auto: update
      ddl-auto: none
